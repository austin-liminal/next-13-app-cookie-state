const links = [
  "/",
  "/server-client-in-memory",
  "/client-with-localstorage",
  "/server-client-cookies",
];

export const PrimaryNav = () => {
  return (
    <nav>
      <ul className="btn-list">
        <li>
          <button className="btn btn-lg"></button>
        </li>
      </ul>
    </nav>
  );
};
